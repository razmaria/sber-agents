# План разработки Telegram-бота с LLM-ассистентом

## 📊 Отчет по прогрессу

| Итерация | Статус | Прогресс | Описание |
|----------|--------|----------|----------|
| 1. Настройка проекта и базовый бот | ⬜ Не начато | 0/7 | Инициализация проекта, конфигурация и Telegram-бот |
| 2. LLM клиент | ⬜ Не начато | 0/2 | Базовый клиент для работы с LLM |
| 3. Интеграция | ⬜ Не начато | 0/2 | Подключение LLM к боту |
| 4. История диалога | ⬜ Не начато | 0/2 | Сохранение контекста разговора |
| 5. Обработка ошибок | ⬜ Не начато | 0/2 | Улучшенная обработка ошибок |
| 6. Команды | ⬜ Не начато | 0/2 | Команды /start и /reset |
| 7. Логирование | ⬜ Не начато | 0/2 | Настройка логирования |
| 8. Финальная полировка | ⬜ Не начато | 0/2 | Makefile и документация |

**Легенда статусов:**
- ✅ Выполнено
- 🟡 В работе
- ⬜ Не начато
- ❌ Заблокировано

---

## Итерация 1: Настройка проекта и базовый бот

**Цель:** Создать базовую структуру проекта, настроить конфигурацию и запустить Telegram-бота.

**Тест:** Бот запускается и отвечает на любое сообщение фиксированным текстом.

- [ ] Инициализировать проект с `uv` (pyproject.toml)
- [ ] Создать структуру папок (`src/`, `docs/`)
- [ ] Настроить `.gitignore` и `.env.example`
- [ ] Создать `src/config.py` для загрузки переменных окружения
- [ ] Добавить валидацию обязательных переменных (TELEGRAM_BOT_TOKEN, OPENROUTER_API_KEY)
- [ ] Создать `src/bot.py` с инициализацией aiogram
- [ ] Создать `src/handlers.py` с обработчиком текстовых сообщений (пока без LLM)

---

## Итерация 2: LLM клиент

**Цель:** Реализовать базовый клиент для работы с LLM через Openrouter.

**Тест:** Клиент отправляет запрос в LLM и получает ответ (можно протестировать отдельно).

- [ ] Создать `src/llm_client.py` с функцией `get_llm_response()`
- [ ] Реализовать отправку запроса в Openrouter API с системным промптом

---

## Итерация 3: Интеграция

**Цель:** Подключить LLM клиент к боту для генерации ответов.

**Тест:** Бот отвечает на сообщения пользователя через LLM (без истории диалога).

- [ ] Подключить `llm_client.py` к обработчику сообщений
- [ ] Реализовать отправку ответа LLM пользователю

---

## Итерация 4: История диалога

**Цель:** Добавить сохранение истории диалога для поддержки контекста разговора.

**Тест:** Бот помнит предыдущие сообщения и отвечает с учётом контекста.

- [ ] Реализовать хранение истории диалога в памяти (dict по chat_id)
- [ ] Добавить передачу всей истории в LLM при каждом запросе

---

## Итерация 5: Обработка ошибок

**Цель:** Улучшить обработку ошибок с понятными сообщениями для пользователя.

**Тест:** При ошибке LLM или сети пользователь получает понятное сообщение, бот не падает.

- [ ] Добавить обработку ошибок LLM API с try/except
- [ ] Добавить обработку сетевых ошибок и таймаутов

---

## Итерация 6: Команды

**Цель:** Добавить команды `/start` и `/reset` для управления ботом.

**Тест:** Команда `/start` приветствует пользователя, `/reset` очищает историю диалога.

- [ ] Реализовать команду `/start` с приветствием
- [ ] Реализовать команду `/reset` для очистки истории диалога

---

## Итерация 7: Логирование

**Цель:** Настроить логирование для отладки и мониторинга работы бота.

**Тест:** В консоль выводятся логи запуска, получения сообщений, запросов к LLM и ошибок.

- [ ] Настроить базовое логирование через `logging` модуль
- [ ] Добавить логирование ключевых событий (сообщения, запросы к LLM, ошибки)

---

## Итерация 8: Финальная полировка

**Цель:** Добавить Makefile для удобства разработки и обновить документацию.

**Тест:** Проект можно запустить через `make run`, документация актуальна.

- [ ] Создать `Makefile` с командами `run`, `install`, `clean`
- [ ] Обновить `README.md` с инструкциями по установке и запуску

---

## Примечания

- Каждая итерация должна быть протестирована перед переходом к следующей
- Принцип KISS: реализуем только необходимое для текущей итерации
- История диалога хранится в памяти и теряется при перезапуске бота
- Системный промпт для роли "Финансовый гид" можно хранить в коде или в конфиге


